all people(associates, clients, owners) in one table; enum's; archive;
add comments in xml-files;
add second-level cache to hibernate; add @CatchExceptions to Factory;
add Factory to Spring; setters has returning this ?; add prefix to security-context;
add security annotations ?; information for INFO logger level; split page in %, not in pixels;
never trust the client;


fill store(frontend) with autoload nested object
fix no Session bug


    //welcome page
    @RequestMapping(value = "/")
    public @ResponseBody ModelAndView getIndex() {
        return new ModelAndView("Index");
    }

    <!-- <bean id="cargoController" class="project.controller.rest.CargoController">
        <property name="aClass" ref="cargo"/>
        <property name="service">
            <bean class="project.model.service.GenericEntityService">
                <property name="data">
                    <bean class="project.model.data.GenericEntityData">
                        <property name="factory" ref="sessionFactory"/>
                        <property name="entityClass" value="project.domain.entity.pojo.cargo.Cargo"/>
                    </bean>
                </property>
            </bean>
        </property>
    </bean>-->


     <dependency>
       <groupId>junit</groupId>
       <artifactId>junit</artifactId>
       <version>3.8.1</version>
       <scope>test</scope>
     </dependency>

